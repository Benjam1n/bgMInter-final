
// export function validator(inputs) {
       
//        inputs.forEach(e=>{
//               // if(e.value === "") {
//               //        alert(`${e.classList[0]} must have valid value`)
//               // }
//               let errorTag = e.nextSibling;
//               errorTag.className = `${e.classList[0]}Error`;
//               errorTag.style.color = "red";
//               let errorMassege = errorTag.innerHTML;
              
//               console.log(e)

//               if(e.classList[0] === "Name") {
//                      if(e.value === "") {
//                             errorTag.innerHTML = `${e.classList[0]} must have a value !`;
       
//                      }
//               }else if (e.classList[0] === "Symbol"){
//                      if(e.value === "") {
//                             errorTag.innerHTML = `${e.classList[0]} must have a value !`;
       
//                      }

//               }else if (e.classList[0] === "Decimal"){
//                      if(e.value === "") {
//                             errorTag.innerHTML = `${e.classList[0]} must have a value ! (9 or 18)`;
       
//                      }else if (e.value === "18" || e.value === "9"){
//                             errorTag.innerHTML = ``;
//                      }else {
//                             errorTag.innerHTML = `${e.classList[0]} must have a value ! (9 or 18)`;
//                      }

//               }else if (e.classList[0] === "TotalSupply"){
//                      if(e.value === "") {
//                             errorTag.innerHTML = `${e.classList[0]} must have a value !`;
       
//                      }

//               }else if (e.classList[0] === "MaxTotalSupply") {
//                      if(e.value === "") {
//                             errorTag.innerHTML = `${e.classList[0]} must have a value !`;
       
//                      }

//               }else if (e.classList[0] === "ownerAddress") {
//                      if(e.value === "") {
//                             errorTag.innerHTML = `${e.classList[0]} must have a value !`;
       
//                      }

//               }else if (e.classList[0] === "burnFee"){
//                      if(e.value === "") {
//                             errorTag.innerHTML = `${e.classList[0]} must have a value !`;
       
//                      }

//               }else if (e.classList[0] === "minBurnFee") {
//                      if(e.value === "") {
//                             errorTag.innerHTML = `${e.classList[0]} must have a value !`;
       
//                      }

//               }else if (e.classList[0] === "maxBurnFee") {
//                      if(e.value === "") {
//                             errorTag.innerHTML = `${e.classList[0]} must have a value !`;
       
//                      }

//               }else if (e.classList[0] === "buyTax") {
//                      if(e.value === "") {
//                             errorTag.innerHTML = `${e.classList[0]} must have a value !`;
       
//                      }

//               }else if (e.classList[0] === "minBuyTax") {
//                      if(e.value === "") {
//                             errorTag.innerHTML = `${e.classList[0]} must have a value !`;
       
//                      }
                     
//               }else if (e.classList[0] === "maxBuyTax") {
//                      if(e.value === "") {
//                             errorTag.innerHTML = `${e.classList[0]} must have a value !`;
       
//                      }
                     
//               }else if (e.classList[0] === "sellTax") {
//                      if(e.value === "") {
//                             errorTag.innerHTML = `${e.classList[0]} must have a value !`;
       
//                      }
                     
//               }else if (e.classList[0] === "minSellTax") {
//                      if(e.value === "") {
//                             errorTag.innerHTML = `${e.classList[0]} must have a value !`;
       
//                      }
                     
//               }else if (e.classList[0] === "maxSellTax") {
//                      if(e.value === "") {
//                             errorTag.innerHTML = `${e.classList[0]} must have a value !`;
       
//                      }
//                      // else if (e.value > 100 || e.value < 0) {
//                      //        errorTag.innerHTML = `${e.classList[0]} must have a valid value !, (0 - 100)`;
//                      // }
                     
//               }else if (e.classList[0] === "marketingwallet") {
//                      if(e.value === "") {
//                             errorTag.innerHTML = `${e.classList[0]} must have a value !`;
       
//                      }
                     
//               }
              
//        })

// }